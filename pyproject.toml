[project]
name = "wargame-mvp"
version = "0.1.0"
description = "LLM WarGame MVP"
requires-python = ">=3.10"

[tool.pyright]
typeCheckingMode = "standard"

[tool.ruff]
line-length = 100

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
